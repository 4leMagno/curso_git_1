**git init** serve para inicializar o git. 
**git status** serve para ver se tem git instalado ou inicializado
comandos para que foram executados: 
git commit -m "Hello World upando o primeiro arquivo"
-m é de mensagem! 
git remote é a origin
git push -u origin main é principal. A raiz do projeto
git add index.html para adicionar 1 arquivo por vez.
para adicionar muitos arquivos ao mesmo tempo 
**git add . **
**git commit -m **é para salva as mudanças com a mensagem
**git push** é para enviar para o github 

